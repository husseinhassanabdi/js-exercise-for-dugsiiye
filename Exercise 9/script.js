

let car = {
    make : "Toyota",
    model : "Camry",
    year : 2020,
    start : function() {
        console.log("The Car has started"); 
    }
};

car.start();

console.log(car.make);
console.log(car.model);
console.log(car.year);